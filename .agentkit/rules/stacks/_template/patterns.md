# Patterns — <stack-name> (template)

These patterns extend:
- `.agentkit/rules/common/patterns.md`

## Preferred patterns (fill for this stack)
- Pattern 1: <name>
  - When to use:
  - When NOT to use:
  - Example location in repo (optional):

- Pattern 2: <name>
  - When to use:
  - When NOT to use:

## Architecture boundaries (stack-specific)
- Layers/modules typical for this stack:
  - <layer A> depends on <layer B> (direction)
- Dependency rules:
  - Allowed:
  - Forbidden:

## Anti-patterns (explicitly avoid)
- <anti-pattern 1>
- <anti-pattern 2>

## Checklist
- [ ] Changes follow preferred patterns for this stack
- [ ] No new anti-patterns introduced
- [ ] Public interfaces are minimal and documented
